# 🚀 त्वरित सेटअप गाइड - अपने लोकलहोस्ट पर AI-PPTX चलाएं

यह गाइड आपके लोकल मशीन पर Gamma AI प्रोजेक्ट को चलाने के लिए सटीक git कमांड के साथ चरण-दर-चरण निर्देश प्रदान करता है।

## 📋 आवश्यक चीजें (Prerequisites)

शुरू करने से पहले, सुनिश्चित करें कि आपके पास है:
- [ ] **Python 3.9 या उच्च** इंस्टॉल है ([डाउनलोड करें](https://www.python.org/downloads/))
- [ ] **MySQL 8.0 या उच्च** इंस्टॉल है ([डाउनलोड करें](https://dev.mysql.com/downloads/))
- [ ] **Git** इंस्टॉल है ([डाउनलोड करें](https://git-scm.com/downloads))
- [ ] **Google Gemini API Key** ([यहाँ से प्राप्त करें](https://makersuite.google.com/app/apikey))

---

## 🔧 चरण-दर-चरण सेटअप

### चरण 1: रिपॉजिटरी क्लोन करें

अपना टर्मिनल/कमांड प्रॉम्प्ट खोलें और चलाएं:

```bash
# रिपॉजिटरी क्लोन करें
git clone https://github.com/Bsoumyaranjan32/AI-PPTX.git

# प्रोजेक्ट डायरेक्टरी में जाएं
cd AI-PPTX

# सत्यापित करें कि आप सही डायरेक्टरी में हैं
pwd  # Windows में: cd
```

### चरण 2: Python Virtual Environment बनाएं

**Windows के लिए:**
```cmd
# Virtual environment बनाएं
python -m venv venv

# Virtual environment को Activate करें
venv\Scripts\activate

# आपको अपने टर्मिनल में (venv) prefix दिखना चाहिए
```

**Linux/Mac के लिए:**
```bash
# Virtual environment बनाएं
python3 -m venv venv

# Virtual environment को Activate करें
source venv/bin/activate

# आपको अपने टर्मिनल में (venv) prefix दिखना चाहिए
```

### चरण 3: Python Dependencies इंस्टॉल करें

```bash
# पहले pip को अपग्रेड करें
pip install --upgrade pip

# सभी आवश्यक पैकेज इंस्टॉल करें
pip install -r requirements.txt

# इंस्टॉलेशन सत्यापित करें
pip list
```

**नोट:** इंस्टॉलेशन में आपके इंटरनेट स्पीड के आधार पर 2-5 मिनट लग सकते हैं।

### चरण 4: Environment Variables सेटअप करें

```bash
# Example environment फ़ाइल को कॉपी करें
cp .env.example .env

# Windows के लिए (यदि cp काम नहीं करता):
copy .env.example .env
```

अब `.env` फ़ाइल को अपने पसंदीदा टेक्स्ट एडिटर से एडिट करें:

```bash
# nano, vim, या कोई भी टेक्स्ट एडिटर उपयोग करें
nano .env

# या Windows पर:
notepad .env
```

**.env में आवश्यक कॉन्फ़िगरेशन:**

```bash
# Database Settings
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=आपका_mysql_password
DB_NAME=gamma_ai
DB_PORT=3306

# Secret keys जनरेट करें (नीचे दिया गया कमांड चलाएं और परिणाम paste करें)
SECRET_KEY=your_secret_key_here
JWT_SECRET_KEY=your_jwt_secret_key_here

# Google Gemini API Key (यहाँ से प्राप्त करें: https://makersuite.google.com/app/apikey)
GOOGLE_GEMINI_API_KEY=आपकी_gemini_api_key

# OpenRouter API Key (वैकल्पिक, DeepSeek के लिए)
OPENROUTER_API_KEY=आपकी_openrouter_api_key

# Windows उपयोगकर्ता: इस लाइन को रखें
GRPC_DNS_RESOLVER=native
```

**सुरक्षित secret keys जनरेट करें:**

```bash
# SECRET_KEY जनरेट करने के लिए यह चलाएं
python -c "import secrets; print(secrets.token_hex(32))"

# JWT_SECRET_KEY जनरेट करने के लिए फिर से चलाएं
python -c "import secrets; print(secrets.token_hex(32))"

# प्रत्येक परिणाम को कॉपी करें और अपनी .env फ़ाइल में paste करें
```

### चरण 5: MySQL Database सेटअप करें

#### विकल्प 1: स्वचालित सेटअप (अनुशंसित)

```bash
# स्क्रिप्ट को executable बनाएं (केवल Linux/Mac)
chmod +x database/init_db.sh

# डेटाबेस सेटअप स्क्रिप्ट चलाएं
./database/init_db.sh

# Windows के लिए, Git Bash उपयोग करें या विकल्प 2 का पालन करें
```

#### विकल्प 2: मैनुअल सेटअप

**चरण 5a: MySQL शुरू करें**

```bash
# जांचें कि MySQL चल रहा है या नहीं
mysql --version

# यदि MySQL नहीं चल रहा है, तो इसे शुरू करें:
# Windows: Services खोलें और MySQL शुरू करें
# Linux: sudo systemctl start mysql
# Mac: brew services start mysql
```

**चरण 5b: Database बनाएं**

```bash
# MySQL में लॉग इन करें
mysql -u root -p
# प्रॉम्प्ट होने पर अपना MySQL पासवर्ड दर्ज करें
```

MySQL shell में:

```sql
-- Database बनाएं
CREATE DATABASE gamma_ai CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- सत्यापित करें कि database बन गया है
SHOW DATABASES;

-- MySQL से बाहर निकलें
EXIT;
```

**चरण 5c: Schema Import करें**

```bash
# डेटाबेस schema import करें
mysql -u root -p gamma_ai < database/schema.sql

# सत्यापित करें कि tables बन गए हैं
mysql -u root -p gamma_ai -e "SHOW TABLES;"
```

आपको tables दिखने चाहिए: `users`, `presentations`, `presentation_shares`, `api_usage_log`

### चरण 6: सेटअप सत्यापित करें

आइए सत्यापित करें कि सब कुछ सही तरीके से कॉन्फ़िगर किया गया है:

```bash
# Python version जांचें
python --version

# जांचें कि virtual environment active है (venv दिखना चाहिए)
which python  # Windows: where python

# जांचें कि .env फ़ाइल मौजूद है
ls -la .env  # Windows: dir .env

# डेटाबेस कनेक्शन जांचें
mysql -u root -p gamma_ai -e "SELECT 'Connection successful!' as Status;"
```

### चरण 7: एप्लिकेशन चलाएं

```bash
# सुनिश्चित करें कि virtual environment active है
# आपको अपने टर्मिनल में (venv) दिखना चाहिए

# Flask एप्लिकेशन शुरू करें
python run.py
```

आपको इस तरह का आउटपुट दिखना चाहिए:
```
 * Serving Flask app 'app'
 * Debug mode: on
WARNING: This is a development server. Do not use it in a production deployment.
 * Running on http://127.0.0.1:5000
Press CTRL+C to quit
```

### चरण 8: एप्लिकेशन एक्सेस करें

अपना वेब ब्राउज़र खोलें और जाएं:

```
http://localhost:5000
```

या

```
http://127.0.0.1:5000
```

आपको Gamma AI होमपेज दिखना चाहिए! 🎉

---

## 🧪 एप्लिकेशन टेस्ट करें

1. **खाता बनाएं:**
   - "Sign Up" पर क्लिक करें
   - Username, email, और password दर्ज करें
   - "Register" पर क्लिक करें

2. **लॉग इन करें:**
   - अपनी credentials का उपयोग करके लॉग इन करें
   - आपको dashboard दिखना चाहिए

3. **प्रेजेंटेशन बनाएं:**
   - "Create New Presentation" पर क्लिक करें
   - एक टॉपिक दर्ज करें (जैसे, "Artificial Intelligence")
   - स्लाइड की संख्या चुनें (जैसे, 5)
   - एक थीम चुनें (जैसे, "dialogue")
   - "Generate" पर क्लिक करें
   - 10-30 सेकंड प्रतीक्षा करें

4. **प्रेजेंटेशन Export करें:**
   - अपनी generated presentation देखें
   - "Export" पर क्लिक करें और PDF, DOCX, या PPTX चुनें
   - फ़ाइल आपके कंप्यूटर पर डाउनलोड हो जाएगी

---

## 🐛 सामान्य समस्याएं और समाधान

### समस्या 1: "python: command not found"

**समाधान:**
```bash
# python3 का उपयोग करने का प्रयास करें
python3 --version

# या python.org से Python इंस्टॉल करें
```

### समस्या 2: "pip: command not found"

**समाधान:**
```bash
# pip इंस्टॉल करें
python -m ensurepip --upgrade

# या python3 का उपयोग करें
python3 -m ensurepip --upgrade
```

### समस्या 3: "Access denied for user 'root'@'localhost'"

**समाधान:**
```bash
# MySQL password रीसेट करें या सही password उपयोग करें
# जांचें कि .env में DB_PASSWORD, MySQL password से मेल खाता है

# मैनुअल रूप से कनेक्शन टेस्ट करें:
mysql -u root -p
```

### समस्या 4: "Can't connect to MySQL server"

**समाधान:**
```bash
# जांचें कि MySQL चल रहा है या नहीं
# Windows: Services जांचें
# Linux: sudo systemctl status mysql
# Mac: brew services list

# यदि नहीं चल रहा है तो MySQL शुरू करें
# Linux: sudo systemctl start mysql
# Mac: brew services start mysql
```

### समस्या 5: "Module not found" errors

**समाधान:**
```bash
# सुनिश्चित करें कि virtual environment active है
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Dependencies फिर से इंस्टॉल करें
pip install -r requirements.txt
```

### समस्या 6: Port 5000 पहले से उपयोग में है

**समाधान:**
```bash
# Port 5000 उपयोग कर रहे process को खोजें और बंद करें
# Linux/Mac:
lsof -ti:5000 | xargs kill -9

# Windows:
netstat -ano | findstr :5000
taskkill /PID <PID_NUMBER> /F

# या .env में port बदलें:
FLASK_RUN_PORT=5001
```

### समस्या 7: "DNS resolution failed" (Windows)

**समाधान:**
सुनिश्चित करें कि आपकी `.env` फ़ाइल में है:
```bash
GRPC_DNS_RESOLVER=native
```

### समस्या 8: "Invalid API key" या "API key not set"

**समाधान:**
```bash
# सत्यापित करें कि आपकी .env फ़ाइल में सही API key है
# यहाँ से नई key प्राप्त करें: https://makersuite.google.com/app/apikey

# सुनिश्चित करें कि key के आस-पास कोई extra spaces नहीं हैं
GOOGLE_GEMINI_API_KEY=your_key_here
```

---

## 📝 त्वरित संदर्भ कमांड

### दैनिक विकास वर्कफ़्लो

```bash
# 1. प्रोजेक्ट में जाएं
cd AI-PPTX

# 2. Virtual environment Activate करें
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# 3. नवीनतम changes pull करें
git pull origin main

# 4. कोई भी नए dependencies इंस्टॉल करें
pip install -r requirements.txt

# 5. एप्लिकेशन चलाएं
python run.py

# 6. काम पूरा होने पर, virtual environment deactivate करें
deactivate
```

### Git कमांड

```bash
# वर्तमान branch जांचें
git branch

# नवीनतम code pull करें
git pull origin main

# रिपॉजिटरी status जांचें
git status

# Commit history देखें
git log --oneline -10

# नई branch बनाएं
git checkout -b feature/my-feature

# Branch switch करें
git checkout main

# Remote से अपडेट करें
git fetch origin
```

### Database कमांड

```bash
# MySQL में लॉगिन करें
mysql -u root -p

# Database backup लें
mysqldump -u root -p gamma_ai > backup.sql

# Database restore करें
mysql -u root -p gamma_ai < backup.sql

# Tables देखें
mysql -u root -p gamma_ai -e "SHOW TABLES;"

# Users देखें
mysql -u root -p gamma_ai -e "SELECT id, username, email FROM users;"
```

---

## 🎯 अगले कदम

सफल सेटअप के बाद:

1. **डॉक्यूमेंटेशन पढ़ें:**
   - Features और API डॉक्यूमेंटेशन के लिए [README.md](README.md) देखें
   - योगदान करना चाहते हैं तो [CONTRIBUTING.md](CONTRIBUTING.md) देखें

2. **अपना सेटअप customize करें:**
   - Images के लिए Google Custom Search कॉन्फ़िगर करें
   - DeepSeek AI model के लिए OpenRouter सेट अप करें
   - Themes और layouts adjust करें

3. **Production में deploy करें:**
   - Production server के लिए Gunicorn का उपयोग करें
   - SSL certificates के साथ HTTPS सेट अप करें
   - Production database कॉन्फ़िगर करें

---

## 💡 सुझाव

- **अपनी API keys को सुरक्षित रखें**: कभी भी `.env` फ़ाइल को git में commit न करें
- **नियमित रूप से अपडेट करें**: नवीनतम features प्राप्त करने के लिए `git pull` चलाएं
- **अपना डेटा backup लें**: नियमित रूप से presentations export करें
- **Virtual environment का उपयोग करें**: चलाने से पहले हमेशा venv activate करें
- **Logs जांचें**: Errors के लिए `pptx_service.log` देखें

---

## 📞 मदद चाहिए?

यदि आप अभी भी फंसे हुए हैं:

1. **मौजूदा issues जांचें**: [GitHub Issues](https://github.com/Bsoumyaranjan32/AI-PPTX/issues)
2. **नया issue बनाएं**: Error messages के साथ अपनी समस्या का वर्णन करें
3. **Discussions में शामिल हों**: [GitHub Discussions](https://github.com/Bsoumyaranjan32/AI-PPTX/discussions)

---

## ✅ सत्यापन Checklist

मदद मांगने से पहले, सत्यापित करें:

- [ ] Python 3.9+ इंस्टॉल है: `python --version`
- [ ] MySQL चल रहा है: `mysql --version`
- [ ] Virtual environment active है: `(venv)` prefix दिख रहा है
- [ ] Dependencies इंस्टॉल हैं: `pip list | grep Flask`
- [ ] .env फ़ाइल मौजूद है और कॉन्फ़िगर है: `cat .env`
- [ ] Database बना है: `mysql -u root -p -e "SHOW DATABASES;"`
- [ ] Tables मौजूद हैं: `mysql -u root -p gamma_ai -e "SHOW TABLES;"`
- [ ] API key सेट है: `.env` फ़ाइल जांचें
- [ ] Port 5000 free है: `lsof -i:5000` (खाली होना चाहिए)

---

**शुभ कोडिंग! 🚀**

यदि आपको यह गाइड मददगार लगी, कृपया रिपॉजिटरी को ⭐ स्टार करें!

---

**English Guide:** [SETUP.md](SETUP.md)
